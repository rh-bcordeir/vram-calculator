# Optional. Leave unset to call huggingface.co directly from the browser.
# Set to /hf in development to route through the Vite proxy, or to your own
# proxy URL if you need to reach gated repos with a server-side token.
# VITE_HF_BASE=/hf
